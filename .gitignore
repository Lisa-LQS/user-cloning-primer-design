# Generated designs: reproducible from the inputs, so not tracked.
designs/

# Lab input files kept out of the shared repo. USER_design_1.xlsx is the shipped example;
# any other USER_design_*.xlsx stays local (the file itself is untouched, just untracked).
USER_design_2.xlsx

# Local editor / tooling state
.claude/
.DS_Store
__pycache__/
*.py[cod]
.venv/
venv/
